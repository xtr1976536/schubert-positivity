# Axiom Audit

This file records the non-Lean mathematical inputs currently used by the
formalization.  The intended final state is:

- `rg "sorry"` returns nothing;
- manuscript-owned propositions are Lean theorems;
- remaining axioms are either genuine external theorems from the cited
  literature or explicitly marked geometric infrastructure still to be replaced.

## Current Lean-Checked Manuscript Steps

The following manuscript steps are already Lean theorems, not axioms:

- permutation part of Lemma `lem:conjugation`;
- Lemma `lem:inversions`, including the counted inversion rectangle;
- coefficientwise positivity extraction from Proposition `prop:coefficient-gw`;
- incidence-cycle effectivity/invariance after the incidence fundamental cycle
  and `ev₃` equivariance inputs;
- Proposition `prop:refined`, coefficient form;
- Proposition `prop:twisted-positive`;
- Theorem `thm:main`, relative to the remaining external/geometric inputs.

## External Literature Axioms

### `lam_shimozono_stable_finite_comparison`

Manuscript label:

- Proposition `prop:stable-finite`.

External source:

- Lam--Shimozono stable quantum double Schubert polynomial theory;
- Kim finite presentation and Schubert basis;
- finite-rank stability under the embeddings `S_m -> S_{m+1}`.

Role:

- Identifies the stable coefficient with a finite twisted coefficient for one
  sufficiently large `n`.

### `mihalcea_projection_duality_coefficient`

Manuscript labels:

- Fact `fact:duality`;
- Fact `fact:projection`;
- Proposition `prop:incidence-expression`.

External source:

- Mihalcea's equivariant Poincare duality and equivariant Gysin/projection
  formula.

Role:

- Converts the twisted Gromov-Witten coefficient into the coefficient of the
  incidence cycle paired with the opposite Schubert class.

### `gao_xiong_refined_graham_positive`

Manuscript labels:

- Proposition `prop:refined`;
- Proposition `prop:twisted-positive`.

External source:

- Gao--Xiong refined Graham positivity theorem.

Lean-proved manuscript input already used:

- Lemma `lem:inversions`, via `manuscript_lem_inversions`;
- counted block-swap inversion rectangle, via
  `manuscript_lem_inversions_count`.

Status:

- This is no longer a top-level axiom in `ExternalTheorems.lean`.
- It is now a Lean theorem routed through
  `gao_xiong_refined_graham_positive_for_cycle` in
  `Geometry/ExternalFacts.lean`.

Role:

- Gives positivity of Schubert coefficients for an effective
  `B^-(tau)`-invariant cycle in the cone generated by the variables `t_i-y_j`.

## Geometric Infrastructure Axioms To Eliminate

These are not acceptable final stopping points. They are temporary interfaces
for geometric infrastructure that we must either formalize or replace by
smaller cited theorems.

### `incidence_theorem_properties`

Manuscript label:

- Theorem `thm:incidence`.

Role:

- Packages reducedness, local irreducibility, and pure codimension for the
  incidence scheme.

Next reduction target:

- Prove using FW--Mihalcea preimage theorem, Kontsevich moduli irreducibility,
  the conjugation lemma, and the expected-codimension pullback formula.

### `incidenceSchemeInvariant`

Manuscript label:

- Proposition `prop:invariance`.

Role:

- States that the incidence scheme is stable under the group `B^-(tau)`.

Next reduction target:

- Prove from stability of `tau X_u`, stability of `X_v`, and equivariance of
  `(ev₁,ev₂)`.

### `incidenceInclusion_equivariant`

Manuscript label:

- Proposition `prop:invariance`.

Role:

- States that the inclusion from the incidence scheme into the stable-map space
  is equivariant.

Next reduction target:

- Prove from `incidenceSchemeInvariant`.

### `stableMapEv3_equivariant`

Manuscript label:

- Proposition `prop:invariance`.

Role:

- States that the third evaluation map on the stable-map space is equivariant.

Next reduction target:

- Prove from the post-composition group action on stable maps.

### `incidenceFundamentalCycleEffective`

Manuscript label:

- Part of Proposition `prop:invariance`.

Status:

- This is now a Lean theorem, not an axiom.

Proved from:

- `incidence_theorem_properties`;
- `fundamental_cycle_effective_of_reduced`.

### `incidenceFundamentalCycleInvariant`

Manuscript label:

- Part of Proposition `prop:invariance`.

Status:

- This is now a Lean theorem, not an axiom.

Proved from:

- `incidenceSchemeInvariant`;
- `fundamental_cycle_invariant_of_invariant_scheme`.

### `ev3_equivariant`

Manuscript label:

- Used in Proposition `prop:invariance`.

Status:

- This is now a Lean theorem, not an axiom.

Proved from:

- `incidenceInclusion_equivariant`;
- `stableMapEv3_equivariant`;
- `equivariant_comp`.

## General Geometry Axioms

These are currently placed in `Geometry/ExternalFacts.lean`:

- `fw_mihalcea_preimage_reduced`;
- `fw_mihalcea_preimage_locally_irreducible`;
- `fw_mihalcea_preimage_expected_codim`;
- `kontsevich_moduli_irreducible`;
- `mihalcea_expected_codim_pullback`;
- `proper_pushforward_effective`;
- `equivariant_pushforward_invariant`;
- `gao_xiong_refined_graham_positive_for_cycle`.

Some are true external theorems; some are standard cycle-theoretic
infrastructure.  The next sprint step is to connect the incidence theorem to
these facts and remove any duplicate or overly coarse interfaces.
