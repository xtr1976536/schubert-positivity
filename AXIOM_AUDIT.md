# Axiom audit

This file records the non-Lean mathematical inputs currently used by the
formalization.  The intended final state is:

- `rg "sorry"` returns nothing;
- every `axiom` below is a genuine external theorem or standard formalism
  imported from the literature;
- every manuscript-owned inference is a Lean theorem built from these
  interfaces.

## Current axioms

### `lam_shimozono_stable_finite_comparison`

Manuscript label:

- Proposition `prop:stable-finite`

External source:

- Lam--Shimozono stable quantum double Schubert polynomial theory;
- Kim finite presentation and Schubert basis;
- finite-rank stability under the embeddings `S_m -> S_{m+1}`.

Role:

- Identifies the stable coefficient with a finite twisted coefficient for one
  sufficiently large `n`.

Reason not formalized yet:

- Requires the full Lam--Shimozono stable polynomial model, finite-rank kernels,
  stable quantization map, and basis theorem.

### `mihalcea_projection_duality_coefficient`

Manuscript labels:

- Fact `fact:duality`;
- Fact `fact:projection`;
- Proposition `prop:incidence-expression`.

External source:

- Mihalcea's equivariant Poincare duality and equivariant Gysin/projection
  formula.

Role:

- Converts the twisted Gromov-Witten coefficient into the coefficient of the
  incidence cycle paired with the opposite Schubert class.

Reason not formalized yet:

- Requires equivariant Chow/cohomology rings, Gysin maps, projection formula,
  and cycle-theoretic pushforward compatibility.

### `gao_xiong_refined_graham_positive`

Manuscript labels:

- Proposition `prop:refined`;
- Proposition `prop:twisted-positive`.

External source:

- Gao--Xiong refined Graham positivity theorem.

Lean-proved manuscript input already used:

- Lemma `lem:inversions`, via `manuscript_lem_inversions`;
- counted block-swap inversion rectangle, via
  `manuscript_lem_inversions_count`.

Role:

- Gives positivity of incidence-cycle coefficients in the cone generated by
  the variables `t_i-y_j`.

Reason not formalized yet:

- Requires the refined Graham theorem for effective `B^-(w)`-invariant cycles
  on the flag variety.

## Non-axiom opaque declarations

The declarations `Perm`, `Degree`, `Coeff`, `StableCoeff`, `TwistedGWCoeff`,
`IncidenceCycleCoeff`, and `CoeffPositive` are abstract carriers and operations.
They are placeholders for the mathematical objects of the manuscript.  They are
not proof assumptions by themselves; the proof assumptions are exactly the
axioms listed above.
